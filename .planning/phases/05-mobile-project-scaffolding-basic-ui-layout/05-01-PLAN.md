---
phase: 05-mobile-project-scaffolding-basic-ui-layout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - android_tv_app/settings.gradle
  - android_tv_app/mobile/build.gradle
  - android_tv_app/mobile/src/main/AndroidManifest.xml
  - android_tv_app/mobile/src/main/java/com/sportstv/mobile/MainActivity.kt
  - android_tv_app/mobile/src/main/res/layout/activity_main.xml
  - android_tv_app/mobile/src/main/res/values/strings.xml
autonomous: true
requirements:
  - TEST-01
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Mobile app module is registered in settings.gradle"
    - "Mobile app build.gradle compiles clean"
    - "MainActivity layout includes a BottomNavigationView"
    - "AndroidManifest defines portrait orientation lock for mobile"
  artifacts:
    - path: "android_tv_app/settings.gradle"
      contains: "include ':mobile'"
    - path: "android_tv_app/mobile/build.gradle"
      contains: "com.android.application"
    - path: "android_tv_app/mobile/src/main/AndroidManifest.xml"
      contains: "screenOrientation=\"portrait\""
    - path: "android_tv_app/mobile/src/main/java/com/sportstv/mobile/MainActivity.kt"
      contains: "class MainActivity"
---

<objective>
Bootstrap the Android Mobile application project structure within the existing Gradle multi-module setup, and implement the basic UI scaffolding with Bottom Navigation and portrait orientation lock.

Purpose: Establishes a clean foundation for mobile-specific development, separating TV (Leanback) and mobile touch-screen modules.
Output: Initial Gradle mobile configuration, Main Activity, strings resource file, and basic layout structure.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@android_tv_app/settings.gradle
@android_tv_app/app/build.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile module to Gradle settings</name>
  <files>android_tv_app/settings.gradle</files>
  <read_first>android_tv_app/settings.gradle</read_first>
  <action>
    Append `include ':mobile'` to `android_tv_app/settings.gradle`.
  </action>
  <verify>
    Verify settings.gradle contains the mobile inclusion.
  </verify>
  <acceptance_criteria>
    - file contains "include ':mobile'"
  </acceptance_criteria>
  <done>Mobile module declared in Gradle settings</done>
</task>

<task type="auto">
  <name>Task 2: Configure mobile build.gradle and Manifest</name>
  <files>android_tv_app/mobile/build.gradle, android_tv_app/mobile/src/main/AndroidManifest.xml</files>
  <read_first>android_tv_app/app/build.gradle</read_first>
  <action>
    1. Create `android_tv_app/mobile/build.gradle`. Configure it as an `application` module similar to the `app` module, but without the Leanback dependencies. Use modern Android material and constraint-layout libraries suited for phone/touch UI.
    2. Create `android_tv_app/mobile/src/main/AndroidManifest.xml`. Define `com.sportstv.mobile` package, launch intent for `MainActivity`, and lock `screenOrientation` to `portrait` for MainActivity. Set `requestLegacyExternalStorage` if needed or keep permissions clean.
  </action>
  <verify>
    Check if build.gradle and AndroidManifest exist and contain expected configuration.
  </verify>
  <acceptance_criteria>
    - build.gradle exists and contains id 'com.android.application'
    - AndroidManifest.xml exists and defines MainActivity with screenOrientation="portrait"
  </acceptance_criteria>
  <done>Mobile module configuration complete</done>
</task>

<task type="auto">
  <name>Task 3: Implement MainActivity and layouts</name>
  <files>android_tv_app/mobile/src/main/java/com/sportstv/mobile/MainActivity.kt, android_tv_app/mobile/src/main/res/layout/activity_main.xml, android_tv_app/mobile/src/main/res/values/strings.xml</files>
  <action>
    1. Create basic strings.xml containing app name and navigation items.
    2. Create `activity_main.xml` layout containing a FrameLayout for content container and a BottomNavigationView for navigation tabs (Home/Categories, Favorites).
    3. Create `MainActivity.kt` class that loads the layout and sets up listeners for navigation selection.
  </action>
  <verify>
    Verify compiling project in android_tv_app using `./gradlew :mobile:assembleDebug`.
  </verify>
  <acceptance_criteria>
    - MainActivity.kt compiles successfully
    - BottomNavigationView exists in activity_main.xml
  </acceptance_criteria>
  <done>Main touch UI layout and Activity implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run Gradle compile command:
  `$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"; ./gradlew :mobile:assembleDebug` from `android_tv_app` directory.
- Verify build status returns successfully.
</verification>

<success_criteria>
- Mobile module compiles clean without errors.
- Scaffolding resources, MainActivity, and manifest are created and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-project-scaffolding-basic-ui-layout/05-01-SUMMARY.md`
</output>
