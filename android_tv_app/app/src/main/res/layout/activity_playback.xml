<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- ExoPlayer surface -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true" />

    <!-- Loading state overlay -->
    <LinearLayout
        android:id="@+id/loading_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:background="#99000000"
        android:visibility="gone">

        <TextView
            android:id="@+id/tv_loading_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:text="Loading..." />

        <ProgressBar
            android:id="@+id/loading_bar"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:indeterminate="true"
            android:indeterminateTint="#E11D48" />
    </LinearLayout>

    <!-- Error state overlay -->
    <LinearLayout
        android:id="@+id/error_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="48dp"
        android:background="#E6000000"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚠️"
            android:textSize="64sp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/tv_error"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="12dp"
            android:text="Playback error." />
            
        <TextView
            android:id="@+id/tv_error_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginBottom="24dp"
            android:text="Retrying in 5..." />

        <Button
            android:id="@+id/btn_retry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔄 Retry Now"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#E11D48"
            android:paddingHorizontal="32dp"
            android:paddingVertical="12dp"
            android:focusable="true" />

    </LinearLayout>

    <!-- Top-right controls: Ticker toggle (auto-hides after a few seconds) -->
    <LinearLayout
        android:id="@+id/top_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/btn_toggle_ticker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📺 Ticker: ON"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:backgroundTint="#AA000000"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:focusable="true"
            android:nextFocusDown="@id/player_view" />

    </LinearLayout>

    <!-- Bottom Overlays: match info + ticker stacked vertically -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical">

        <!-- Match info overlay (fades after 4 sec) -->
        <LinearLayout
            android:id="@+id/match_info_overlay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:background="#AA000000"
            android:alpha="1.0">

            <TextView
                android:id="@+id/tv_match_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="28sp"
                android:textStyle="bold"
                android:shadowColor="#000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="3" />

            <TextView
                android:id="@+id/tv_match_sub"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#CCFFFFFF"
                android:textSize="16sp"
                android:layout_marginTop="4dp" />

        </LinearLayout>

        <!-- News Ticker (smooth animated via ObjectAnimator, not marquee) -->
        <FrameLayout
            android:id="@+id/ticker_container"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="#CC000000"
            android:clipChildren="true"
            android:visibility="visible">



            <!-- Scrolling ticker text (Native Marquee) -->
            <TextView
                android:id="@+id/ticker_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:singleLine="true"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:scrollHorizontally="true"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:textStyle="bold"
                android:text="@string/ticker_message" />

        </FrameLayout>

    </LinearLayout>

</FrameLayout>
